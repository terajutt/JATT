services:
  - type: web
    name: jatt-airlines
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: TWOFACTOR_API_KEY
        sync: false